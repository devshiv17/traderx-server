# Simplified requirements - avoid packages that need Rust compilation
fastapi==0.104.1
uvicorn==0.24.0
motor==3.3.2
pymongo==4.6.0

# Use older pydantic that definitely has wheels
pydantic==2.0.3

python-jose==3.3.0
passlib==1.7.4
python-multipart==0.0.6
python-dotenv==1.0.0
websockets==12.0
aiohttp==3.9.1

# Angel One SmartAPI dependencies
pyotp==2.9.0
logzero==1.7.0
websocket-client==1.6.4

# Skip problematic packages for now
# pycryptodome - skip if not essential
# email-validator - skip if not essential
# smartapi-python - might be causing issues