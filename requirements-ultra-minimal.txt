# Ultra minimal - only packages with guaranteed pre-built wheels
fastapi==0.95.2
uvicorn==0.22.0
motor==3.1.2
pymongo==4.6.0
pydantic==1.10.12
python-multipart==0.0.6
python-dotenv==1.0.0
aiohttp==3.8.5

# Skip all problematic packages for initial deployment
# Can add back later once basic app is running